procedure fib(let k) {
    if (k < 2) { 
        return 1; 
    } else { 
        return fib(k-1) + fib(k-2); 
    } 
}
let x := 1;
let arr[10];
while (x < 10) {
    arr[x] := fib(x);
    x := x + 1;
}